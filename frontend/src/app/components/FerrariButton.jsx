import { Button } from "@mui/material";

export const FerrariButton = (props) => {
    
    return (
        <Button variant="contained"  style={{width:'200px', height:'50px', backgroundColor:'red', margin:'30px'}}>Allocate Orders</Button>
    );
};